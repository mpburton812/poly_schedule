# Archived Stitch reference exports

This folder contains static HTML exports from early Stitch design explorations. They are **reference-only** and are not wired into the running PWA.

The live app uses `index.html`, `css/styles.css`, and `js/` modules. Do not edit these HTML files expecting runtime changes—update the main app instead.
